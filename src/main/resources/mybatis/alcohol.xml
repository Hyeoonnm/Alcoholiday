<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="alcohol">

    <select id="list" resultType="Alcohol">
        SELECT *
        FROM alcohol
        <where>
            <if test="kind == 1">
               stuff_num = #{keyword}
            </if>

            <if test="kind == 2">
                stuff_name LIKE CONCAT(CONCAT('%',#{keyword}),'%')
            </if>

            <if test="kind == 3">
                stuff_publisher like CONCAT(CONCAT('%',#{keyword}),'%')
            </if>

            <if test="kind == 4">
                stuff_price like CONCAT(CONCAT('%',#{keyword}),'%')
            </if>
        </where>
        ORDER BY stuff_num
    </select>

    <insert id="add">
        INSERT INTO alcohol (stuff_name, stuff_publisher, stuff_price, stuff_reg_date)
        VALUES (#{stuffName}, #{stuffPublisher}, #{stuffPrice}, NOW())
    </insert>


    <select id="item" resultType="Alcohol">
        SELECT *
        FROM alcohol
        WHERE stuff_num = #{stuffNum}
    </select>

    <update id="update">
        UPDATE alcohol
        SET stuff_name      = #{stuffName},
            stuff_publisher = #{stuffPublisher},
            stuff_price     = #{stuffPrice},
            stuff_reg_date  = NOW()
        WHERE stuff_num = #{stuffNum}
    </update>

    <delete id="delete">
        DELETE
        FROM alcohol
        WHERE stuff_num = #{stuffNum}
    </delete>

</mapper>